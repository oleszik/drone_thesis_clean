Phase 1 — Build the safety gate first

Goal: nothing autonomous can start unless the vehicle and app are actually ready.

Tell Codex to do this first:

Add /api/readiness
Aggregate checks into one object:
mavlink connected
heartbeat age
gps ok
ekf ok
battery ok
rc present
armed state
mode
home set
fence configured
mission inside fence
backend config ready
Return:
overall_ready
can_manual
can_autonomous
checks[]
blocking_reasons[]

Why first: right now you only have partial health/status, not a real readiness gate.

Codex prompt 1

Audit the current FastAPI backend and implement a new readiness service for real-flight safety.

Requirements:
- Add GET /api/readiness
- Reuse existing mavlink/status/telemetry services where possible
- Aggregate these checks into one structured response:
  - mavlink_connected
  - heartbeat_age_sec
  - gps_ok
  - ekf_ok
  - battery_ok
  - rc_link_ok
  - home_position_set
  - current_mode
  - armed
  - fence_configured
  - mission_within_fence
  - backend_config_ok
- Response shape should include:
  - overall_ready
  - can_manual
  - can_autonomous
  - checks: array of {key, ok, severity, message, value}
  - blocking_reasons: string[]
  - timestamp
- Put logic in a dedicated readiness_service.py if clean, otherwise organize clearly in main.py
- Keep code production-clean with type hints and error handling
- Do not break existing endpoints
- At the end, show all changed files and explain how to test /api/readiness
Phase 2 — Build a dedicated “Real Test” page

Goal: one operator page for field use, not a dev dashboard.

Tell Codex to:

Add a new page like RealTest.jsx
Show a large readiness checklist
Show live top bar:
mode
armed
gps
ekf
battery
home set
fence status
heartbeat age
Color-code:
green = ready
yellow = manual only
red = blocked
Add a big status banner:
“Manual only”
“Autonomy blocked”
“Ready for tiny mission”

Why second: your current UI has status and controls, but not a proper checklist page.

Codex prompt 2

Create a dedicated real-flight operator page in the React frontend.

Requirements:
- Add a new page/component for real testing, e.g. src/pages/RealTest.jsx
- Use existing REST/SSE data sources and the new GET /api/readiness endpoint
- Show:
  - overall readiness banner
  - step-by-step checklist from readiness checks
  - live vehicle status cards: mode, armed, GPS, EKF, battery, RC link, home, fence, heartbeat age
- Color states:
  - green = fully ready
  - yellow = manual only
  - red = blocked
- Keep layout simple, field-friendly, large buttons/text, low cognitive load
- Do not remove the current dashboard; add this as a dedicated route/page
- At the end, show changed files and explain how to navigate to the page
Phase 3 — Add hard autonomy lockouts

Goal: even if the frontend bugs out, the backend still refuses unsafe starts.

Tell Codex to:

Block mission generation/start if readiness is not green
Return HTTP 409 with machine-readable reason
Apply the block to:
mission generation
mission upload/start
SITL/real scan start
any future autonomy endpoint

Why third: this is where your system stops being a demo and becomes field-safe. Your own summary already pointed to backend guards on mission generation/start.

Codex prompt 3

Add hard backend safety guards so unsafe autonomous actions are rejected server-side.

Requirements:
- Before any autonomous mission generation/start endpoint runs, check readiness
- If readiness is not sufficient, reject with HTTP 409
- Include structured JSON:
  - error
  - blocking_reasons
  - readiness_snapshot
- Apply this to all relevant endpoints, including mission generation and scan/autonomy start endpoints
- Frontend should disable those buttons when readiness is not green, but backend protection must be authoritative
- Keep manual safety actions like RTL/Land available even when readiness is bad
- Show all changed files and list every guarded endpoint
Phase 4 — Add explicit Hold button and backend Hold action

Goal: one-click “stop and hold here.”

Loiter is the right implementation target because ArduPilot Loiter is the mode that holds current location, heading, and altitude.

Tell Codex to:

Add POST /api/control/hold
Internally map it to LOITER
Add a big Hold button on the Real Test page
Keep RTL and Land beside it

Why fourth: for first flights, this matters more than replay or polish.

Codex prompt 4

Implement an explicit Hold action for the vehicle.

Requirements:
- Add POST /api/control/hold in the FastAPI backend
- Use the existing mavlink mode-change path and map Hold to LOITER
- Return a clean structured response with success/failure and resulting mode
- Add a prominent Hold button to the new Real Test page
- Place Hold next to RTL and Land
- Do not remove the existing set_mode endpoint; this is a dedicated safety helper
- Show changed files and explain how Hold behaves


Phase 5 — Add a tiny mission preset

Goal: first real autonomous test is tiny, reversible, and boring.

Use a preset like:

takeoff to 3 m
hold 8–10 s
move forward 3 m
hold 8–10 s
RTL

This is much safer than starting with scan or full mission logic. Your current code does not have a tiny mission mode yet.

Codex prompt 5

Implement a tiny mission preset for first real-flight testing.

Requirements:
- Add a backend tiny mission builder/preset
- Mission profile:
  - takeoff to low configurable altitude (default 3m)
  - short hover
  - short forward move (default 3m)
  - short hover
  - RTL
- Add clear clamps:
  - max altitude
  - max horizontal displacement
  - low speed cap
- Expose it through a dedicated endpoint
- Add a "Tiny Mission" action on the Real Test page
- This action must still respect readiness and fence checks
- Show all changed files and explain how to test it safely


Phase 6 — Add strict backend fence validation and clamping

Goal: no mission can exist outside your allowed area.

QGroundControl Plan View supports geofence planning, so your app should mirror that logic on the backend and reject anything outside the allowed boundary.

Your current backend only partially validates mission geometry, not strict operational fence inclusion.

Tell Codex to:

Add allowed operation fence config
Check all mission points against it
Reject with clear message if outside
Optionally add a clamp mode later, but start with reject

Codex prompt 6

Implement strict backend geofence validation for all mission and path generation.

Requirements:
- Add configurable allowed operating fence/polygon support
- Validate mission areas, generated waypoints, start positions, and tiny mission targets against that fence
- If any point is outside the fence, reject with a clear error
- Do not silently allow out-of-bounds paths
- Expose fence status to /api/readiness
- Frontend Real Test page should show whether fence is configured and whether the current mission is inside it
- Show changed files and describe the validation flow


Phase 7 — Add command queueing, rate limiting, and timeouts

Goal: prevent duplicate clicks, command spam, or stale commands.

Your current command path has no explicit rate limiting or timeout protection.

Tell Codex to:

Add per-vehicle command mutex/queue
Enforce minimum spacing between risky actions
Add timeout for:
arm
takeoff
mode change
mission start
Reject duplicate conflicting commands while one is active

Codex prompt 7

Add command safety infrastructure around vehicle control actions.

Requirements:
- Introduce per-vehicle command serialization/locking
- Add minimum spacing between risky control commands
- Add timeout handling for arm, takeoff, mode change, mission start, and RTL initiation
- Reject or coalesce duplicate conflicting requests
- Return structured command-state responses
- Keep implementation lightweight and easy to reason about
- Show changed files and explain the new command flow
Phase 8 — Add automatic run bundle export

Goal: every real test leaves a complete evidence package.

Your code already supports exports, but not automatic bundling after each run.

Tell Codex to:

On run stop, auto-save:
JSON
CSV
GeoJSON
PDF report
Save into a timestamped run folder
Add a “recent runs” endpoint
Show the latest bundle in UI

Codex prompt 8

Implement automatic run bundling for every completed run.

Requirements:
- When a run stops, automatically create a timestamped bundle directory
- Save:
  - run json
  - path csv
  - geojson
  - pdf report
- Reuse existing RunManager export helpers where possible
- Add an endpoint to list recent run bundles
- Add a simple UI panel showing the latest saved runs
- Show changed files and explain where bundles are stored
Phase 9 — Add post-flight replay page

Goal: after every flight, replay exactly what happened.

Your current system has track/export data but no dedicated replay page.

Tell Codex to:

Build Replay.jsx
Load saved runs
Animate track over time
Show event markers:
arm
takeoff
mode changes
hold
rtl
landing
warnings

Codex prompt 9

Create a post-flight replay page in the React frontend.

Requirements:
- Add a page to browse saved runs
- Load run bundle data from the backend
- Replay track position over time on the map
- Show event markers for key actions and warnings
- Include play/pause/scrub controls and a simple timeline
- Keep the implementation robust and minimal
- Show changed files and explain how replay data is loaded
Phase 10 — Final laptop/operator polish

Goal: your laptop becomes a field station, not a dev machine.

Tell Codex to:

Add one startup script for backend + frontend
Add one field checklist page as default route option
Add a connection watchdog banner
Add offline/error states
Add a “field mode” UI with:
large buttons
low clutter
dark mode friendly
Add a preflight summary print/export page

Codex prompt 10

Polish the app for field use on a laptop.

Requirements:
- Add a field-friendly startup/dev script workflow
- Improve operator UX for the Real Test page:
  - large buttons
  - strong status banners
  - clear offline/error states
  - low clutter
- Add a printable/exportable preflight summary view from readiness data
- Make the operator flow obvious: Readiness -> Arm/Takeoff -> Tiny Mission -> Hold/RTL -> Review Run
- Show changed files and describe the final operator workflow
The exact build sequence I would use

Do it in this order and do not skip ahead:

/api/readiness
Real Test page
backend autonomy lockouts
Hold button + endpoint
tiny mission preset
strict fence validation
command queue/rate limits/timeouts
auto log bundle
replay page
laptop field polish
The stopping rule

Do not do real autonomous flights until phases 1 through 7 are complete.

Phases 8 through 10 are not the first line of safety, but they are what make the system feel finished and trustworthy.


Phase 8A — serial MAVLink transport support

Tell Codex to upgrade the MAVLink service so it supports real telemetry radios over serial, not just SITL-friendly flows.

Codex prompt

Upgrade the backend MAVLink connection layer so it supports real telemetry radios over serial for a Matek H743-WLite + 3DRRadio X6-433MHz setup.

Requirements:
- Support MAVLink connection over:
  - serial device path (examples: /dev/ttyUSB0, COM7)
  - UDP SITL fallback
- Add explicit connection modes:
  - sitl_udp
  - serial_radio
  - disconnected
- Add configurable settings for:
  - serial_port
  - serial_baud
  - reconnect_interval_sec
  - heartbeat_timeout_sec
  - telemetry_stale_timeout_sec
- Implement clean connect/disconnect/reconnect behavior
- Track:
  - current connection mode
  - connection state
  - last heartbeat timestamp
  - heartbeat age
  - last telemetry timestamp
  - error state
- Expose this through backend APIs without breaking current SITL workflows
- Keep the code production-clean with type hints and clear separation of concerns
- At the end, show changed files and explain how to run in serial radio mode vs SITL mode
Phase 8B — radio connection endpoints

Add endpoints for operator control.

Codex prompt

Add backend endpoints for managing MAVLink radio connections.

Requirements:
- Add endpoints such as:
  - GET /api/connection
  - POST /api/connection/connect
  - POST /api/connection/disconnect
  - POST /api/connection/test_heartbeat
- GET /api/connection should return:
  - mode
  - connected
  - serial_port
  - serial_baud
  - last_heartbeat_age_sec
  - last_telemetry_age_sec
  - stale
  - error
- POST /api/connection/connect should accept either serial radio config or SITL config
- POST /api/connection/test_heartbeat should attempt to verify MAVLink heartbeat and return a structured result
- Preserve existing health/status endpoints
- Show all changed files and example request/response payloads
Phase 8C — radio section in the Real Test page

Add a clear operator panel.

Codex prompt

Add a radio telemetry connection panel to the Real Test page.

Requirements:
- Add a dedicated UI section for MAVLink connection source
- Show:
  - connection mode
  - serial port
  - baud rate
  - connected/disconnected
  - heartbeat age
  - telemetry age
  - stale status
  - error message
- Add controls:
  - select port
  - select baud
  - connect
  - disconnect
  - test heartbeat
- Make it field-friendly and easy to understand
- Integrate it with the readiness banner and checklist
- Do not clutter the page
- At the end, show changed files and explain the operator flow
Phase 8D — readiness integration for radio comms

Make radio health part of the preflight gate.

Codex prompt

Integrate radio telemetry status into the readiness system.

Requirements:
- Extend /api/readiness so it includes radio-specific checks:
  - connection_mode_selected
  - serial_port_configured
  - connection_open
  - mavlink_heartbeat_present
  - telemetry_not_stale
- These checks must affect:
  - can_manual
  - can_autonomous
  - overall_ready
- If in serial_radio mode and telemetry is missing or stale, autonomous actions must be blocked
- Manual safety actions like RTL/Land/Hold should remain visible in the UI but clearly warn about stale telemetry
- Show changed files and the final readiness response example
Phase 8E — telemetry-loss handling

This is important for real flights.

Codex prompt

Implement telemetry-loss handling for real-flight radio connections.

Requirements:
- Detect loss of heartbeat and stale telemetry during operation
- Emit clear backend state transitions:
  - healthy
  - degraded
  - lost
- Record telemetry loss/restoration events in run logs
- Prevent new autonomous commands from being sent when telemetry is stale or lost
- Keep the UI updated in near real time using existing SSE mechanisms where possible
- Add clear warnings on the Real Test page
- Show all changed files and explain the state machine